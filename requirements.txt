# STEP - Python dependencies
#
# PyTorch with CUDA support should be installed separately per the
# instructions at https://pytorch.org, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# (the paper was trained with PyTorch 2.5 + CUDA 12.1; a CUDA GPU is recommended - training falls back to CPU if none is available)

torch>=2.0
numpy==1.26.4
scipy>=1.10
scikit-learn>=1.2
faiss-gpu
tqdm
tensorboard
